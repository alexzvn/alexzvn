{
  "name": "@jm/audio",
  "version": "0.1.0",
  "private": true,
  "description": "Native Audio-Capture-Bindings (PortAudio + ASIO-Host) für die JM Production Suite — Mehrkanal-Aufnahme (z. B. Dante Virtual Soundcard) in Float32-planar.",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "install": "node scripts/maybe-build.mjs",
    "rebuild": "node-gyp rebuild",
    "spike": "node test/spike.cjs"
  },
  "dependencies": {
    "bindings": "^1.5.0",
    "node-addon-api": "^8.0.0"
  },
  "devDependencies": {
    "node-gyp": "^10.0.0"
  }
}
